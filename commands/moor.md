---
description: Launch the moor diff viewer, or manage its CLI install
argument-hint: "[<left> <right> | --context <path> <left> <right> | install-cli [--dir <path>] | completions zsh | --version]"
---

Run the moor CLI with the user's arguments.

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/moor" $ARGUMENTS
```
